#include <iostream>
#include <cstdio>
#include <string>
#include <algorithm>
using namespace std;

void process(string& s, int& a, int& b, int& c){
    for(int i = 0; i < s.length(); ++i){
        if()
    }
}

int main(){

    string s1, s2;

    cin >> s1 >> s2;
    int a1, b1, c1, a2, b2, c2;
    process(s1, a1, b1, c1);
    process(s2, a2, b2, c2);

}
